/** Fixed TapStamp classic look — the default generic wallet pass. */

export const TAPSTAMP_BRAND = {
  backgroundColor: 'rgb(26, 24, 20)',
  foregroundColor: 'rgb(201, 169, 110)',
  labelColor: 'rgb(138, 128, 112)',
} as const;

/** Clean light card used when AI designs for a shop (closer to premium wallet references). */
export const AI_SHOP_BRAND_FALLBACK = {
  backgroundColor: 'rgb(255, 255, 255)',
  foregroundColor: 'rgb(30, 64, 120)',
  labelColor: 'rgb(120, 130, 145)',
} as const;
